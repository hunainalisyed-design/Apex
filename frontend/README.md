# Apex frontend

Next.js (App Router) application. See the repo root [`README.md`](../README.md) for setup and the full dev workflow (this app is run from there via `npm run dev` at the repo root, not standalone).
