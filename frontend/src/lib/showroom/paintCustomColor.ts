/** Reserved PAINT option assetRef marking the "Custom Color" sentinel row — ExteriorPanel
 * and the build summary (Spec 9, AC-2) both special-case it against the live
 * customPaintHex rather than the catalog swatch. */
export const CUSTOM_COLOR_ASSET_REF = "paint-custom";
